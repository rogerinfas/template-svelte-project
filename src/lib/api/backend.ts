import createClient from "openapi-fetch";
// import type { paths } from "./types/api";

/*
const client = createClient<paths>({ baseUrl: "http://localhost:4000/api-json" });
export default client;
*/
